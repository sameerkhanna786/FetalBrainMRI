/*
 * Local HTMX asset placeholder for the offline FastAPI/Jinja scaffold.
 * Vendor a pinned htmx.min.js release here before making the Python UI the
 * primary runtime.
 */

window.htmx = window.htmx || { version: "local-placeholder" };
